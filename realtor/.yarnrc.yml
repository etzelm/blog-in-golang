# Yarn 4 (Berry) config. Keep the traditional node_modules layout (no PnP)
# to minimize migration surface — Vite + jsdom + the existing yarn-install
# build flow all work as before with this. Telemetry off (no behavior impact).
nodeLinker: node-modules
enableTelemetry: false
